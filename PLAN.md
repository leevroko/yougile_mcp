# PLAN: YouGile MCP Server

**Цель**: Замена легаси OpenClaw-скиллов на Go MCP-сервер для ИИ-ассистента

---

## 1. API Reference

Полный справочник эндпоинтов, DTO и форматов данных: [`API_REFERENCE.md`](API_REFERENCE.md)

**Источник**: https://ru.yougile.com/api-json (официальный OpenAPI spec, загружен 2026-05-19)
**Base URL**: `https://ru.yougile.com/api-v2`
**Auth**: `Authorization: Bearer {apiKey}`
**Rate Limit**: 50 req/min

Группы эндпоинтов:
- Auth (4 эндпоинта) — получение/управление API-ключами
- Users (6 эндпоинтов) — CRUD сотрудников
- Company (2 эндпоинта) — детали и изменение компании
- Projects (4 эндпоинта) — CRUD проектов
- Project Roles (5 эндпоинтов) — управление ролями и правами
- Departments (4 эндпоинта) — CRUD отделов
- Boards (4 эндпоинта) — CRUD досок
- Columns (4 эндпоинта) — CRUD колонок
- Tasks (7 эндпоинтов) — CRUD задач, чат-подписчики, два list-эндпоинта (прямой/обратный порядок)
- Stickers (старые, 3 эндпоинта) — кастомные поля (string/select/number/date/user)
- String Stickers (новые, 7 эндпоинтов) — dropdown-стикеры с состояниями
- Sprint Stickers (новые, 7 эндпоинтов) — спринт-стикеры с датами

Пагинация: ручное управление `offset += limit` пока `paging.next === true`.
Формат: `PagingMetadata { count, limit, offset, next }`.

Известные неопределённости (требуют экспериментальной проверки):
- `/companies{*companyId}` — URL без слеша между `companies` и ID
- `stickers.custom` в `StickersDto` доски — формат не специфицирован
- Взаимосвязь `/stickers` и `/string-stickers`
- Формат тела ошибок (400/401/404/429)
- Наличие `paging` в BoardListDto, ColumnListDto

---

## 2. Определение пользовательских сценариев (business-first)

> ⛔ **Блокирующий шаг. Дизайн DDD-сущностей (шаг 3) не начинается, пока сценарии не определены и не согласованы.**

> ✅ **Статус: выполнен (шаг 2).** Спецификации сценариев — [`SCENARIOS.md`](SCENARIOS.md).

Сценарии определяются **до** дизайна модели: DDD-сущности и агрегаты выстраиваются под типичные бизнес-задачи ИИ-ассистента, а не наоборот.

Типичные сценарии работы:

- **Bulk move** — перемещение группы задач между колонками (например, все просроченные → Review)
- **Summarize** — сводка по доске (группировка, метрики, рекомендации) — замена daily_review.py
- **Audit** — поиск просрочки, задач без стикеров, автоматическое перемещение
- **Goal tracking** — отслеживание прогресса целей (weighted KR по стикерам Weight и Progress)
- **Quick actions** — быстрое создание/перемещение/обновление задач
- **Batch stickers** — массовое обновление стикеров у группы задач
- **Compression** — цепочка daily → weekly → monthly → yearly review

Для каждого сценария определить:
- Входные/выходные данные
- Какие API-эндпоинты нужны
- Какие сущности участвуют (уточняются на шаге 3)
- Граничные случаи

**Результат**: Документированные спецификации сценариев (в этом файле или отдельном). Основа для дизайна DDD-сущностей (шаг 3).

### Стоимость сценариев в API-запросах

Оценка по легаси-скриптам и OpenAPI spec:

| Сценарий | Запросы (сейчас) | Избыточность |
|----------|------------------|--------------|
| List tasks | 9 (колонки + 8× задачи по колонкам) | структура доски грузится каждый раз |
| Summarize | 9–17 (8 колонок + стикеры + пагинация) | то же + повторная расшифровка стикеров |
| Audit | 9 + N×PUT | то же + N мутаций |
| Goal tracking | 9–10 | то же + повторная расшифровка стикеров |
| Quick actions | 1 | минимальна |
| Batch stickers | N×PUT | нет batch-эндпоинта |
| Compression | 9–17 | то же, что Summarize |

Источники избыточности:
- Каждый сценарий заново грузит структуру доски (колонки, стикеры, проекты) — меняется редко, но дёргается всегда
- N запросов по колонкам вместо одного (см. API_REFERENCE.md раздел 19, п. 6 — кандидат на проверку `GET /tasks?boardId=` без columnId)
- Расшифровка стикеров: задача хранит `{stickerUUID: value}`, нужна карта доски `GET /stickers?boardId=` (1 кэшируемый запрос)
- Нет batch-эндпоинтов: bulk move / batch stickers = N×PUT неизбежно

### Принципы оптимизации (согласовано)

> Основная единица стоимости — не API-запрос, а **вызов MCP-инструмента** (1 вызов = 1 раунд LLM + вброс результата в контекст). Оптимизация направлена на сокращение вызовов инструментов, а не только запросов.

1. **Гибрид двух слоёв инструментов**
   - Слой 1 — тонкий CRUD: `list_projects`, `list_boards`, `list_columns`, `list_tasks`, `get_task`, `create_task`, `update_task`, `get_stickers` — точечные операции и отладка
   - Слой 2 — композитные (1 вызов покрывает весь сценарий): `get_board_snapshot`, `summarize_board`, `audit_board`, `track_goals`, `bulk_move_tasks`, `batch_update_stickers`, `compress_reviews`
2. **Вложенные справочные данные**: list-инструменты возвращают названия колонок + легенду стикеров прямо в ответе (агент не дёргает `list_columns`/`get_stickers` отдельно)
3. **Формат вывода**: параметр `format: json|markdown`. Аналитика → markdown по умолчанию (TL;DR сверху 3–5 строк, детали ниже), CRUD → JSON. Сервис всегда возвращает структурированные данные; markdown — отдельный презентационный слой с шаблонами
4. **Пагинация — всегда полные данные**: композитные проходят все страницы (`offset += limit` пока `next === true`). Чинит баг легаси (терял данные при >100 задач). Приоритет — точность, токены вторичны
5. **Дельта-обновления**: параметр `since` (timestamp) — возврат только изменённых задач; для периодического опроса агента
6. **Без TTL-кэша на старте** — добавить позже по реальной нагрузке

### MCP-инструменты (целевые)

| MCP Tool | Слой | Описание | Формат |
|----------|------|----------|--------|
| `list_projects` | 1 (CRUD) | Список проектов | JSON |
| `list_boards` | 1 (CRUD) | Список досок проекта | JSON |
| `list_columns` | 1 (CRUD) | Список колонок доски | JSON |
| `list_tasks` | 1 (CRUD) | Задачи доски/колонки + названия колонок + легенда стикеров | json\|markdown |
| `get_task` | 1 (CRUD) | Задача по ID | JSON |
| `create_task` | 1 (CRUD) | Создание задачи | JSON |
| `update_task` | 1 (CRUD) | Обновление задачи (перемещение, стикеры, дедлайн) | JSON |
| `get_stickers` | 1 (CRUD) | Легенда стикеров доски | JSON |
| `get_board_snapshot` | 2 (композитный) | Полное состояние доски: колонки, задачи, стикеры, пользователи | json\|markdown |
| `summarize_board` | 2 (композитный) | Сводка: TL;DR + метрики + группировка + рекомендации | json\|markdown |
| `audit_board` | 2 (композитный) | Аудит: просрочка, отсутствие стикеров, авто-перемещение | json\|markdown |
| `track_goals` | 2 (композитный) | Прогресс целей (weighted KR) | json\|markdown |
| `bulk_move_tasks` | 2 (композитный) | Массовое перемещение задач | JSON |
| `batch_update_stickers` | 2 (композитный) | Массовое обновление стикеров | JSON |
| `compress_reviews` | 2 (композитный) | Сжатие ревью (daily→weekly→...) | markdown |

> Все композитные инструменты принимают `since` для дельта-обновлений.

---

## 3. Дизайн DDD сущностей (на основе сценариев)

> ⛔ **Блокирующий шаг. Никакая реализация не начинается, пока этот этап не завершён полностью.**

> ✅ **Статус: выполнен (шаг 3).** Дизайн-документ — [`DESIGN.md`](DESIGN.md).

Определить, опираясь на сценарии из шага 2:

- **Value Objects** — идентификаторы (TaskID, BoardID, и т.д.), заголовки, дедлайны, прогресс, приоритеты, цвета колонок/задач, роли пользователей, типы стикеров, PagingMetadata
- **Entities** — проект, доска, колонка, задача, стикер (старый), string-стикер, sprint-стикер, пользователь
- **Aggregates** — доска с колонками, задача с подзадачами, цель с KR (weighted key results)
- **Repository Interfaces** — контракты для каждого aggregate

**Результат**: Go-файлы с типами и интерфейсами в `internal/domain/`. Модель выстроена под типичные сценарии работы (шаг 2).

---

## 4. Дизайн доменных сервисов

> ✅ **Статус: выполнен (шаг 4).** Дизайн-документ — [`SERVICES.md`](SERVICES.md).

Определить сервисы на основе DDD-сущностей и пользовательских сценариев:

- **BoardService** — CRUD досок и колонок, получение конфигурации доски, snapshot
- **TaskService** — CRUD задач, массовые операции (move, update stickers), ListTasks с вложенными справочными
- **ReviewService** — аналитика доски: группировка, метрики, рекомендации, since
- **AuditService** — поиск просрочки, задач без стикеров, авто-перемещение в Review (rules, dryRun)
- **GoalService** — отслеживание прогресса целей, weighted average KR
- **CompressionService** — цепочка сжатия daily→weekly→monthly→yearly

> **Решение**: вместо Event Bus — **прямые вызовы** между сервисами (принцип Simplicity First, без спекулятивной асинхронности). Таблица связей — в SERVICES.md §9.

**Результат**: Go-интерфейсы сервисов (в SERVICES.md).

---

## 5. Дизайн кодовых решений

> ✅ **Статус: реализовано (MVP).** Код — в `cmd/` и `internal/`.

Архитектурные и технические решения (все реализованы):

- **Go Package Structure** — модуль `github.com/yougile-mcp`, структура директорий по DDD: `internal/domain`, `internal/infrastructure`, `internal/service`, `internal/mcp`
- **HTTP-клиент** — кастомный RoundTripper: Bearer auth → Token bucket rate limiter (50 req/min) → Retry с exponential backoff → HTTP transport (все в `internal/infrastructure/http`)
- **Слой репозиториев** — интерфейсы в domain, HTTP-реализации в infrastructure. Обработка пагинации, маппинг HTTP-статусов в доменные ошибки (`internal/infrastructure/repository`)
- **Экспериментальная верификация API** — ⏳ не выполнена: неопределённости из шага 1 (раздел 19 API_REFERENCE.md) требуют реальных запросов
- **MCP-интеграция** — 15 инструментов двух слоёв (8 CRUD + 7 композитных), format json|markdown, markdown-рендер (TL;DR), дельта `since`, dryRun (`internal/mcp`)
- **Обработка ошибок** — доменные ошибки в `internal/domain/domainerr`, маппинг HTTP-статусов (404→ErrNotFound, 429→ErrRateLimited, ...)
- **Конфигурация** — `YOUGILE_API_KEY`, `YOUGILE_BASE_URL`, `YOUGILE_MEMORY_DIR` через переменные окружения

**Результат**: реализован MVP MCP-сервера (сборка, тесты, запуск — см. Quick Start в AGENTS.md).

---

## 6. Дальнейшее планирование

> ⏳ **Не завершено.** Планирование итераций — следующий шаг после проверки MVP на реальном API.

Разбить дальнейшую работу на итерации:

1. **Экспериментальная верификация API** — проверить неопределённости (раздел 19 API_REFERENCE.md): `GET /tasks?boardId=` без columnId, формат ошибок, `stickers.custom`
2. **Интеграционные тесты** — с реальным API (создание/перемещение задач)
3. **Итерации реализации** — порядок сущностей (User → Project → Board → Column → Task → Sticker)
4. **Критерии готовности** каждого этапа
5. **CI/CD, линтеры, pre-commit хуки** — оценить необходимость

**Результат**: Дорожная карта (roadmap) с конкретными сроками и вехами.
